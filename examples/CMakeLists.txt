# Notice name prefix of this variable, set by CMake according
# to value given with "project()" in the root CMakeLists.txt.
include_directories(${Embedded_CV_SOURCE_DIR}/examples/inc)
include_directories(${Embedded_CV_SOURCE_DIR}/examples/helpers/libjpeg/include)
include_directories(${Embedded_CV_SOURCE_DIR}/inc)
